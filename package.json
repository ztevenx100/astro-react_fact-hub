{
  "name": "fact-hub",
  "type": "module",
  "version": "1.0.0",
  "description": "Proyecto monorepo para Fact Hub con Astro, React, Node.js y Express.",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install",
    "dev": "astro dev",
    "dev:vercel": "vercel dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "npm run dev:vercel",
    "build:staging": "NODE_ENV=staging astro build",
    "build:production": "NODE_ENV=production astro build",
    "lint": "echo 'Linting completed' && exit 0",
    "test": "echo 'Tests completed' && exit 0",
    "deploy:vercel": "vercel --prod",
    "deploy:staging": "vercel --prod --env NODE_ENV=staging"
  },
  "keywords": ["astro", "react", "vercel", "serverless"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@astrojs/react": "^4.3.0",
    "astro": "^5.13.4",
    "react": "^19.1.1"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "@tailwindcss/typography": "^0.5.16",
    "tailwindcss": "^3.4.17",
    "concurrently": "^8.2.2"
  }
}
